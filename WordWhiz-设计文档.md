## WordWhiz — macOS 全局文案优化工具 设计文档

---

### 一、产品概述

WordWhiz 是一款 macOS 原生轻量级工具应用，常驻系统菜单栏运行。用户在任意应用中复制文本后，通过全局快捷键 `⌘ .` 即可一键调用 LLM 大语言模型对剪贴板中的文案进行优化，优化结果在独立面板中实时流式呈现，支持二次编辑、对比查看和一键复制。

**核心价值主张：** 三步完成文案优化——复制、按键、粘贴，打破应用边界的智能写作助手。

**目标用户：** 日常需要大量文字输出的知识工作者，包括产品经理、运营人员、程序员（写文档/邮件）、自媒体创作者等。

**技术选型：** SwiftUI + macOS 原生开发，充分利用系统级能力（全局快捷键注册、剪贴板读取、NSPanel、Keychain 加密存储）。

---

### 二、应用架构

#### 2.1 应用形态

WordWhiz 采用 **Menu Bar App（菜单栏应用）** 形态，不在 Dock 中显示图标（用户可在设置中打开 Dock 图标），启动后在菜单栏右侧显示一个品牌图标"Ww"。这种形态的优势在于：常驻后台不干扰用户工作，随时可用但不占用屏幕空间，符合 macOS 工具型应用的设计惯例（类似 Bartender、CleanShot 等）。

#### 2.2 核心模块划分

整个应用由四个核心模块构成：

**菜单栏入口模块** 负责提供快速访问菜单，显示服务状态（已就绪/连接中/异常）、当前默认指令、最近优化记录条数、快捷键开关等信息，同时提供进入偏好设置的入口。

**优化面板模块** 是用户与 LLM 交互的主界面，承载指令选择、原文展示、结果展示与编辑、对比视图、操作按钮等功能。该面板使用 NSPanel（或 NSFloatingPanel）实现，具有浮动在所有窗口之上的特性，支持拖拽移动和记忆位置。

**后台服务模块** 负责监听全局快捷键、读取系统剪贴板中的文本内容、管理与 LLM API 的网络通信（包括流式 SSE 响应的解析）、维护优化历史的本地持久化存储。

**设置模块** 提供完整的偏好设置窗口，包括通用设置、模型配置、自定义 Prompt 管理、历史记录管理、快捷键配置等。

---

### 三、界面设计详解

#### 3.1 菜单栏图标与下拉菜单

菜单栏图标使用品牌色（#5B7FFF）圆角矩形底色 + 白色 "Ww" 字母，尺寸 18×18pt，确保在 Retina 和普通屏幕上清晰可辨。

点击图标弹出的下拉菜单包含以下项目（从上到下）：

"打开优化面板"右侧标注快捷键 `⌃Z`，这是最常用的操作入口。紧随其后是分隔线，然后是状态指示行——左侧一个绿色（正常）/橙色（连接中）/红色（异常）圆点加文字说明当前服务状态。"最近优化记录"右侧显示未读数量角标。再一道分隔线后是"偏好设置..."（快捷键 `⌘ ⇧ ,`）。下方是"启用全局快捷键"的开关（Toggle Switch），最后通过分隔线隔开"退出 WordWhiz"选项（`⌘ Q`）。

#### 3.2 优化面板（核心界面）

优化面板是一个独立的浮动窗口，默认尺寸 400×560pt，从屏幕右侧滑入（可在设置中更改为左侧或屏幕中央）。面板采用深色主题设计（与 macOS 暗色模式一致），视觉上保持轻量感。面板结构从上到下分为五个区域：

**标题栏区域（Panel Header）**：左侧是品牌标识（Logo 图标 + "WordWhiz" 文字），右侧是一组工具按钮——设置入口、固定窗口（Pin）。点击 Pin 按钮锚定面板后，用户可以拖动面板到任意位置，该位置会被记住并在下次使用快捷键时恢复。解除锚定后，面板会按照设置中的"面板弹出位置"选项出现。标题栏高度约 44pt，提供拖拽移动面板的把手区域。

**指令选择器（Prompt Selector）**：横向排列的标签页（Tab Bar），展示所有自定义指令（应用首次启动时自带 6 个默认指令：✨ 润色、🌍 翻译、📋 摘要、📝 扩写、👔 正式化、💬 口语化）。当前选中指令以品牌蓝色高亮，支持左右滚动以适配更多指令。用户可以在设置中拖拽调整指令顺序，顺序会实时同步到优化面板。所有指令平权，用户可以编辑、删除任何指令，包括初始自带的指令。

**原文区域（Source Section）**：带"原文"标签和"收起/展开"按钮，展示通过 Accessibility API 捕获的选中文本。默认显示最多 3 行（约 80px 高度），超出部分通过底部渐隐效果暗示还有更多内容。收起状态下仅显示 1 行。这个区域的目的是让用户确认捕获的文本是否正确。

**结果区域（Result Section）**：这是面板最核心的区域，占据面板的主要空间。标签"优化结果"右侧显示字数统计。使用一个可编辑的 TextEditor 展示优化结果，背景色略深于面板底色以区分区域。生成完毕后文本自动全选，方便用户直接复制。用户可以在此直接修改措辞、增删内容。

**操作栏（Action Bar）**：底部操作栏左侧放置"重新生成"按钮（🔄），右侧放置"全选文本"按钮和主操作按钮"📋 复制结果"（品牌蓝色实心按钮，视觉权重最高）。

#### 3.3 加载状态与流式输出

当 LLM 正在处理请求时，面板进入加载状态：

结果区域初始显示三条 Skeleton 骨架屏动画（从浅到深的 shimmer 效果），暗示内容正在加载。当第一个 token 返回时，骨架屏消失，开始逐字显示文本，文末有一个蓝色闪烁光标。标签栏显示"● 生成中..."（蓝色），标题栏右侧出现红色"■ 停止"按钮。"复制结果"按钮在生成期间置灰不可用。

生成完毕后，光标消失，状态切换为"✓ 生成完成"（绿色），结果文本自动全选，复制按钮恢复可用。如果用户中途点击停止，状态显示"⏸ 已停止"（橙色），已生成的部分内容保留，用户仍可复制和编辑。

#### 3.4 偏好设置窗口

偏好设置采用经典的 macOS 设置窗口布局——左侧侧边栏导航 + 右侧内容区，窗口尺寸 640×520pt。

**通用设置页** 包含以下可配置项：开机自启动（Toggle）、显示 Dock 图标（Toggle）、优化后自动复制到剪贴板（Toggle）、保留优化历史（Toggle）、面板弹出位置（下拉选择：屏幕右侧/左侧/屏幕中央）。当面板被锚定（Pin）后，用户可以拖动面板到任意位置，该位置会被记住，下次使用快捷键时面板会出现在上次拖动后的位置，直到解除锚定为止。

**模型配置页** 支持选择 LLM 服务商（OpenAI、Anthropic Claude、DeepSeek、通义千问、自定义 OpenAI 兼容接口），填写 API Key（密码输入框，实际存储在 macOS Keychain 中加密保存），自定义 API Base URL，模型名称，以及"测试连接"按钮用于验证配置是否正确。

**自定义指令页** 以卡片列表的形式展示所有指令。每张卡片显示名称（带自定义 emoji 前缀）和 Prompt 内容预览（最多 2 行），右上角有编辑和删除按钮。Prompt 模板中使用 `{{text}}` 作为原文占位符，实际调用时会被替换为捕获的选中文本。列表支持拖拽排序，顺序会实时同步到优化面板。列表底部有一个虚线边框的"+ 添加新指令"按钮。应用首次启动时自带 6 个默认指令，但这些指令与用户自行添加的指令完全平权，用户可以编辑、删除或重新排序。

**历史记录页** 顶部有搜索框支持全文检索，下方以时间线形式展示优化记录，每条记录显示使用的指令标签（彩色小标签）、时间戳和结果文本预览（2 行）。点击可查看完整的原文和优化结果，支持重新使用该结果。

**快捷键设置页** 列出所有可配置的快捷键：触发优化（`⌃Z`）、复制结果（`⌘ ⇧ C`）、切换指令（`⌘ 1-9`）、重新生成（`⌘ R`）、关闭面板（`Esc`）、打开设置（`⌘ ⇧ ,`）。每个快捷键右侧显示当前绑定的按键组合，点击可重新录入。

**关于页** 居中展示应用 Logo、名称、版本号和简短描述，底部有"检查更新"按钮。

---

### 四、核心使用流程

#### 4.1 标准优化流程

第一步：**复制文本**。用户在任意 macOS 应用（备忘录、微信、浏览器、VSCode、Pages 等）中选中需要优化的文本，按 `⌘ C` 复制到剪贴板。

第二步：**按下快捷键 `⌃Z`**。WordWhiz 后台服务监听到全局快捷键触发，立即读取系统剪贴板中的文本内容。如果剪贴板为空或不是文本格式，弹出系统通知提示"剪贴板中没有文本内容"。

第三步：**面板弹出 & 流式生成**。优化面板从预设位置（默认屏幕右侧）以滑入动画出现，原文区域立即显示从剪贴板读取的文本，结果区域开始显示 Skeleton 加载动画，同时后台将原文拼接当前选中指令的 Prompt 模板发送给 LLM API。随着 SSE 流式响应的逐步返回，结果区域开始逐字显示优化文本。

第四步：**查看与编辑**。生成完毕后，结果文本自动全选（蓝色高亮），用户可以直接 `⌘ C` 复制，也可以先在文本框中修改不满意的措辞。

第五步：**复制 & 使用**。用户点击面板底部的"📋 复制结果"按钮或按 `⌘ ⇧ C`，优化后的文案被写入系统剪贴板。按钮短暂变为"✅ 已复制"以确认操作，用户切换回原应用按 `⌘ V` 粘贴即可。

第六步：**（可选）不满意则重新生成**。点击"🔄"按钮或按 `⌘ R`，使用相同的原文和指令重新调用 LLM，获得不同的优化结果。

完成后，面板可通过 `Esc` 键隐藏，也可保持常驻以便后续使用。优化记录自动保存至历史列表。

#### 4.2 指令切换流程

面板打开后，用户可以在指令选择器中点击不同的标签来切换指令。切换指令后，如果当前已有原文内容，会自动用新指令重新发起优化请求，无需手动触发。快捷键 `⌘ 1` 到 `⌘ 9` 可以快速切换到对应位置的指令（按设置中的排序）。

#### 4.3 指令管理流程

用户在偏好设置 → 自定义指令页中可以管理所有指令。应用首次启动时自带 6 个默认指令（润色、翻译、摘要、扩写、正式化、口语化），但这些指令与用户自行添加的指令完全平权。

用户可以进行以下操作：
- **添加新指令**：点击"+ 添加新指令"，输入指令名称和 Prompt 内容（需包含 `{{text}}` 占位符）
- **编辑指令**：点击任意指令的编辑按钮，修改名称或 Prompt 内容
- **删除指令**：点击删除按钮移除指令（包括初始自带的指令）
- **排序指令**：拖拽卡片调整顺序，顺序会实时同步到优化面板

所有指令在优化面板中按设置中的排序展示，快捷键 `⌘ 1` 到 `⌘ 9` 对应排序后的前 9 个指令。

---

### 五、默认指令 Prompt 方案

应用首次启动时自带 6 个默认指令，每个指令对应一套 System Prompt + User Prompt 的组合。这些指令与用户自行添加的指令完全平权，用户可以编辑或删除。以下是默认指令的 Prompt 设计：

**✨ 润色**：System Prompt 为"你是一位专业的中文文案编辑。请对用户提供的文本进行润色优化，修正语法错误、改善措辞表达、提升文字质量，但保持原文核心意思不变。输出仅包含优化后的文本，不需要解释修改原因。" User Prompt 为原文内容。

**🌍 翻译**：支持自动检测源语言。如果原文是中文则翻译为英文，反之翻译为中文。

**📋 摘要**：将长文本压缩为核心要点，保留关键信息，输出简洁精炼的摘要。

**📝 扩写**：在保持原意的基础上，丰富细节、增加论据、扩展表述，使内容更加充实完整。

**👔 正式化**：将口语化、随意的表达转换为正式、规范的书面语，适用于商务邮件、官方文件等场景。

**💬 口语化**：将生硬、书面化的表达转换为自然、亲切的口语风格，适用于社交媒体、日常沟通等场景。

用户可以在设置中添加、编辑、删除或重新排序这些指令。

---

### 六、技术实现要点

#### 6.1 全局快捷键

使用 `HotKey` 开源库来实现全局快捷键绑定。该库基于 Carbon `RegisterEventHotKey` API，无需辅助功能权限即可工作，支持自定义快捷键组合。

#### 6.2 读取剪贴板内容

通过 macOS 系统剪贴板（`NSPasteboard.general`）直接读取用户最近复制的文本内容。支持多种文本格式（`NSStringPboardType`、`public.utf8-plain-text`、`public.utf16-plain-text`）。此方案无需辅助功能权限，兼容所有应用。如果剪贴板为空或不是文本格式，提示用户先复制文本。

#### 6.3 LLM API 集成

使用 `URLSession` 配合 `AsyncBytes` 实现 SSE（Server-Sent Events）流式响应的实时解析。网络层封装为协议（Protocol），便于支持多家 LLM 服务商（OpenAI 兼容接口、Anthropic 原生接口等）。请求超时设置为 30 秒，支持用户手动取消正在进行的请求。

#### 6.4 数据存储

API Key 存储在 macOS Keychain 中，确保安全。用户配置和历史记录使用 SwiftData（或 Core Data）进行本地持久化。自定义 Prompt 模板以 JSON 格式存储在应用沙盒的 Application Support 目录中。

#### 6.5 面板窗口

使用 `NSPanel`（floatingPanel 级别）实现悬浮面板，设置 `styleMask` 包含 `.nonactivatingPanel`，确保面板出现时不会夺走原应用的焦点。面板位置支持拖拽调整并记忆最后位置。面板的出现和消失使用 `NSAnimationContext` 实现平滑的滑入/滑出动画。

---

### 七、权限与首次启动引导

#### 7.1 所需系统权限

WordWhiz 需要一项关键的系统权限——**辅助功能（Accessibility）权限**，用于注册全局快捷键和读取其他应用中的选中文本。

#### 7.2 首次启动引导流程

第一次打开 WordWhiz 时，展示一个简洁的欢迎引导窗口，分为两步：

第一步"欢迎使用 WordWhiz"——简要介绍产品功能，展示核心操作的动画演示："在任意应用中复制文本 → 按下 ⌘ . → 查看优化结果"。

第二步"配置 API"——引导用户选择 LLM 服务商并填写 API Key，提供各平台获取 Key 的链接，以及"测试连接"按钮验证配置。

完成后引导用户"复制这段文字 → 按下 ⌘ . "进行首次体验。

---

### 八、未来扩展方向

后续版本可考虑的功能扩展包括：支持用户自行配置不同快捷键对应不同的优化指令（如 `⌘ 1` 直接触发润色、`⌘ 2` 直接触发翻译）以跳过指令选择步骤；支持批量优化（从剪贴板或文件读取多段文本）；接入本地 LLM（如 Ollama）实现离线使用；增加团队共享的 Prompt 模板库；提供 Safari/Chrome 扩展实现更深度的浏览器集成；可选的 Accessibility API 集成（用于实现原地替换文本功能，需用户授权）。